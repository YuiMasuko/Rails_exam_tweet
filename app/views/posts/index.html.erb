<h1>Tweet一覧</h1>
<p><%= notice %></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= link_to "ブログを編集する", edit_post_path(post.id) %></td>
    </tr>
  <% end %>
  <%= link_to '新しく投稿する', new_post_path %>
</table>
