<h3>以下の内容で投稿しますか？</h3>
<%= form_with(model:@post, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>内容<%= @post.content %></p>
  <%= form.submit  "投稿します" %>
  <%= link_to '戻る', name: 'back' %>
<% end %>
